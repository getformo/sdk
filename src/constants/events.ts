export enum Event {
  PAGE = 'page_hit',
  IDENTIFY = 'identify',  
  CONNECT = 'connect',
  DISCONNECT = 'disconnect',
  CHAIN_CHANGED = 'chain_changed',
  SIGNATURE = 'signature',
  TRANSACTION = 'transaction',
  CUSTOM = 'custom_event',
}
