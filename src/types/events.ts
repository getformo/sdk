export enum SignatureStatus {
  REQUESTED = 'requested',
  REJECTED = 'rejected',
  CONFIRMED = 'confirmed',
}

export enum TransactionStatus {
  STARTED = 'started',
  REJECTED = 'rejected',
  BROADCASTED = 'broadcasted',
}